name: Development Branch Pull Request Workflow
on:
    pull_request: 
        branches: 
            - develop
            - feature/*
jobs:
    terraform-plan:
        uses: ./.github/workflows/terraform-plan.yml
        permissions: 
            contents: read
            pull-requests: write
        with:
            environment: development
